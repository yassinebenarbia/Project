## Where to copy

Copy this folder to the Server/VPS

## Command to run

Under the specified folder, run this command `nix develop` and then you can start listening for incomming images to the server running the following `python main.py`
